# poc-github-auth
test out login via github's api


packages and why i need them

node - to run the server js code
express - to make it easier to do routing
octokit - Github's SDK so I can allow logins via Github
dotenv - loads environment variables into process.env
cors - middleware that can be used to enable CORS with various options
